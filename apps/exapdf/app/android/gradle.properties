org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# AGP 9 의 내장 Kotlin 은 아직 쓸 수 없다.
# 켜면 KGP 를 적용하는 플러그인이 전부 터지는데, 지금 생태계의 Flutter 플러그인은
# 거의 다 KGP 를 적용한다(file_selector_android 등). Flutter 가 안내하는 대로 꺼 둔다
android.builtInKotlin=false

# Windows 에서 Kotlin 증분 컴파일 캐시락으로 빌드가 멈추는 것을 막는다
# (저장소 루트 CLAUDE.md 12-3)
kotlin.incremental=false
